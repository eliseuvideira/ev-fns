{
  "name": "@ev-fns/auth",
  "version": "0.3.2",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "license": "MIT",
  "files": [
    "lib/**/*"
  ],
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "rm -rf lib && yarn build"
  },
  "devDependencies": {
    "@types/express": "^4.17.12",
    "@types/node": "^15.12.4",
    "express": "^4.17.1"
  },
  "dependencies": {
    "@ev-fns/endpoint": "^0.1.1",
    "@ev-fns/errors": "^0.1.0"
  },
  "keywords": [
    "auth",
    "express",
    "middleware",
    "bearer"
  ],
  "peerDependencies": {
    "express": "^4.17.1"
  }
}
